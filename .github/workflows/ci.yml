# SPDX-FileCopyrightText: Copyright (c) 2025 - 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
# SPDX-License-Identifier: Apache-2.0
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
# http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

# NVALCHEMI Toolkit CI Workflow
#
# This workflow runs linting, pytest unit testing, and coverage reporting.
#
# TRIGGERS:
# - Push to main branch or pull-request branches
# - Merge group events (when PRs are merged via merge queue)
# - Scheduled runs (daily at 7 AM UTC)
#
# WORKFLOW OVERVIEW:
# 1. changed-files: Detects which files have changed compared to main branch
# 2. lint: Runs static code checks and linting via pre-commit
# 3. get-pr-labels: Retrieves PR labels for conditional job execution
# 4. test: Runs pytest unit tests with coverage (on 1-GPU runner). UMA is
#    tested from a separate CUDA 12 environment because it conflicts with MACE
#    and the build group (gated on UMA changes or full runs).
# 5. test-multigpu: Runs the `@pytest.mark.multigpu` tests on a 2-GPU runner.
#    They auto-skip in job 4, so this is the only tier that executes them.
#    Gated on DD-relevant changes, the nightly run, or the `ci:multigpu` label.
# 6. verify-status: Verifies all jobs completed successfully

name: "CI"

on:
  push:
    branches:
      - main
      - "pull-request/[0-9]+"
  merge_group:
    types: [checks_requested]
  schedule:
    - cron: "0 7 * * *" # Runs at 7 AM UTC daily

defaults:
  run:
    shell: bash -x -e -u -o pipefail {0}

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

env:
  UV_CACHE_DIR: /tmp/uv-cache
  PRE_COMMIT_HOME: /tmp/pre-commit-cache
  CUDA_EXTRA: cu13
  OPTIONAL_EXTRAS: mace aimnet ase pymatgen
  CI_UV_EXTRAS: "--extra cu13 --extra mace --extra aimnet --extra ase --extra pymatgen"

jobs:
  # ============================================================================
  # CHANGED FILES DETECTION
  # ============================================================================
  changed-files:
    runs-on: ubuntu-latest
    outputs:
      any_changed: ${{ steps.changed-files.outputs.any_changed }}
      uma_changed: ${{ steps.uma-changed.outputs.any_changed }}
      dd_changed: ${{ steps.dd-changed.outputs.any_changed }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          submodules: "recursive"

      - name: Get merge-base commit
        id: merge-base
        run: |
          MERGE_BASE=$(git merge-base HEAD origin/main 2>/dev/null || echo "HEAD~1")
          echo "merge-base=$MERGE_BASE" >> $GITHUB_OUTPUT
          echo "Merge-base commit: $MERGE_BASE"

      - uses: step-security/changed-files@95b56dadb92a30ca9036f16423fd3c088a71ee94 # v46
        id: changed-files
        with:
          base_sha: ${{ steps.merge-base.outputs.merge-base }}
          files: |
            **
            !**.md
            !.github/**
            !.gitignore
            .github/workflows/ci.yml

      # UMA lives in its own environment, so its tests run in a separate CI
      # step. Detect UMA-specific changes to gate that step on PRs.
      - uses: step-security/changed-files@95b56dadb92a30ca9036f16423fd3c088a71ee94 # v46
        id: uma-changed
        with:
          base_sha: ${{ steps.merge-base.outputs.merge-base }}
          files: |
            nvalchemi/models/uma.py
            nvalchemi/models/__init__.py
            nvalchemi/_optional.py
            test/models/test_uma.py
            test/distributed/model/test_uma_multigpu.py
            test/distributed/model/test_uma_gp_partition_multigpu.py
            examples/advanced/09_uma_nve.py
            pyproject.toml
            uv.lock
            .github/workflows/ci.yml

      # The 2-GPU runner is scarcer and its tests slower, so gate that job on
      # the paths those tests cover rather than on any change.
      - uses: step-security/changed-files@95b56dadb92a30ca9036f16423fd3c088a71ee94 # v46
        id: dd-changed
        with:
          base_sha: ${{ steps.merge-base.outputs.merge-base }}
          files: |
            nvalchemi/distributed/**
            nvalchemi/models/**
            nvalchemi/dynamics/**
            test/distributed/**
            test/dynamics/test_distributed_correctness.py
            test/conftest.py
            test/test_multigpu_marker.py
            .github/workflows/ci.yml

      - name: Show output
        run: |
          echo '${{ toJSON(steps.changed-files.outputs) }}'
          echo 'uma_changed=${{ steps.uma-changed.outputs.any_changed }}'
          echo 'dd_changed=${{ steps.dd-changed.outputs.any_changed }}'

  # ============================================================================
  # LINTING STAGE
  # ============================================================================
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      # Install uv via its pinned, checksum-verified installer rather than the
      # astral-sh/setup-uv action, which the org action allow-list blocks
      # (non-GitHub, not in the permitted patterns). Mirrors the test job.
      - name: Setup UV
        env:
          UV_VERSION: "0.9.25"
          UV_CHECKSUM: "10fb1f54d56f3eb60622006797339d4ea0bfda9b358d07db635f73cf89f7094c"
        run: |
          UV_INSTALLER=$(mktemp)
          curl --proto '=https' --tlsv1.2 -LsSf "https://astral.sh/uv/${UV_VERSION}/install.sh" -o "$UV_INSTALLER"
          echo "${UV_CHECKSUM}  ${UV_INSTALLER}" | sha256sum -c -
          sh "$UV_INSTALLER"
          rm "$UV_INSTALLER"
          echo "$HOME/.local/bin" >> $GITHUB_PATH

      - name: Cache pre-commit
        uses: actions/cache@v4
        with:
          path: ~/.cache/pre-commit
          key: pre-commit-${{ hashFiles('.pre-commit-config.yaml') }}
          restore-keys: |
            pre-commit-

      - name: Install dependencies and run lint
        run: |
          uv sync ${CI_UV_EXTRAS}
          make lint

      - name: Verify UMA CUDA resolutions
        run: |
          uv export --quiet --frozen --extra uma-cu12 --extra ase --no-group build \
            --output-file /tmp/uma-cu12-requirements.txt
          uv export --quiet --frozen --extra uma-cu13 --extra ase --no-group build \
            --output-file /tmp/uma-cu13-requirements.txt

  # ============================================================================
  # GET PR LABELS (for copy-pr-bot integration)
  # ============================================================================
  get-pr-labels:
    runs-on: ubuntu-latest
    outputs:
      labels: ${{ steps.get-labels.outputs.labels || steps.get-labels-empty.outputs.labels }}
    steps:
      - name: Get PR number from branch
        if: startsWith(github.ref, 'refs/heads/pull-request/')
        id: get-pr-num
        run: |
          PR_NUM=$(echo ${{ github.ref_name }} | grep -oE '[0-9]+$')
          echo "pr_num=$PR_NUM" >> $GITHUB_OUTPUT

      - name: Get PR labels
        id: get-labels
        if: startsWith(github.ref, 'refs/heads/pull-request/')
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          LABELS=$(gh api repos/${{ github.repository }}/pulls/${{ steps.get-pr-num.outputs.pr_num }} --jq '[.labels[].name]' || echo "[]")
          echo "labels=$LABELS" >> $GITHUB_OUTPUT
          echo "Retrieved labels: $LABELS"

      - name: Set empty labels for non-PR branches
        if: ${{ !startsWith(github.ref, 'refs/heads/pull-request/') }}
        id: get-labels-empty
        run: |
          echo "labels=[]" >> $GITHUB_OUTPUT
          echo "Set empty labels for non-PR branch"

  # ============================================================================
  # TEST STAGE (GPU Runner)
  # ============================================================================
  test:
    needs:
      - lint
      - get-pr-labels
      - changed-files
    runs-on: linux-amd64-gpu-h100-latest-1
    # Headroom for the extra UMA environment install (a second torch + the
    # fairchem stack) when the UMA steps run; well under this otherwise.
    timeout-minutes: 45
    container:
      image: nvcr.io/nvidia/cuda:13.1.0-runtime-ubuntu24.04
      options: --gpus all
    env:
      # Determine if this is a full test run (main/schedule/merge_group) or selective (PR)
      IS_FULL_RUN: ${{ github.ref == 'refs/heads/main' || github.event_name == 'schedule' || github.event_name == 'merge_group' }}
    if: |
      (github.event_name == 'schedule') ||
      contains(fromJSON(needs.get-pr-labels.outputs.labels || '[]'), 'ciflow:all') ||
      (
        !contains(fromJSON(needs.get-pr-labels.outputs.labels || '[]'), 'ciflow:skip') &&
        (needs.changed-files.outputs.any_changed == 'true')
      ) ||
      (
        (github.event_name == 'merge_group') &&
        (needs.changed-files.outputs.any_changed == 'true')
      )
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install apt requirements
        env:
          DEBIAN_FRONTEND: "noninteractive"
          TZ: "Etc/UTC"
        run: |
          apt-get update && \
          apt-get install -y curl \
            git \
            build-essential

      - name: Setup UV
        env:
          UV_VERSION: "0.9.25"
          UV_CHECKSUM: "10fb1f54d56f3eb60622006797339d4ea0bfda9b358d07db635f73cf89f7094c"
        run: |
          UV_INSTALLER=$(mktemp)
          curl --proto '=https' --tlsv1.2 -LsSf "https://astral.sh/uv/${UV_VERSION}/install.sh" -o "$UV_INSTALLER"
          echo "${UV_CHECKSUM}  ${UV_INSTALLER}" | sha256sum -c -
          sh "$UV_INSTALLER"
          rm "$UV_INSTALLER"
          echo "$HOME/.local/bin" >> $GITHUB_PATH

      - name: Install dependencies
        run: |
          export PATH="$HOME/.local/bin:$PATH"
          uv sync ${CI_UV_EXTRAS}

      # UMA conflicts with MACE and the build group, so it uses a dedicated
      # CUDA 12 environment. ``ase`` is required by the UMA test module.
      - name: Install UMA CUDA 12 environment (.venv-uma-cu12)
        if: needs.changed-files.outputs.uma_changed == 'true' || env.IS_FULL_RUN == 'true'
        run: |
          export PATH="$HOME/.local/bin:$PATH"
          uv python install 3.12
          UV_PROJECT_ENVIRONMENT=.venv-uma-cu12 uv sync --frozen --python 3.12 \
            --extra uma-cu12 --extra ase --no-group build

      - name: Verify UMA CUDA 12 environment
        if: needs.changed-files.outputs.uma_changed == 'true' || env.IS_FULL_RUN == 'true'
        run: |
          export PATH="$HOME/.local/bin:$PATH"
          UV_PROJECT_ENVIRONMENT=.venv-uma-cu12 uv run --no-sync python -c "
          import importlib.metadata as md, sys, torch
          fairchem = md.version('fairchem-core')
          print(f'python={sys.version.split()[0]} fairchem-core={fairchem} torch={torch.__version__} cuda={torch.version.cuda}')
          if sys.version_info[:2] != (3, 12):
              sys.exit(f'expected Python 3.12, found {sys.version.split()[0]}')
          if not fairchem.startswith('2.22.'):
              sys.exit(f'expected fairchem-core 2.22.x, found {fairchem}')
          if not torch.__version__.startswith('2.13.0+cu126'):
              sys.exit(f'expected torch 2.13.0+cu126, found {torch.__version__}')
          if not str(torch.version.cuda).startswith('12.'):
              sys.exit(f'expected a CUDA 12 torch build, found {torch.version.cuda}')
          if not torch.cuda.is_available():
              sys.exit('expected a visible CUDA device')
          "

      # ========================================================================
      # TESTMON + COVERAGE CACHING (PR runs only)
      # ========================================================================
      # For PRs: restore cached testmon database and baseline coverage from main
      # This enables selective test execution based on code changes

      - name: Restore testmon cache
        if: env.IS_FULL_RUN != 'true'
        uses: actions/cache/restore@v4
        with:
          path: |
            .testmondata
            .testmondata-shm
            .testmondata-wal
          key: testmon-main-${{ github.sha }}
          restore-keys: |
            testmon-main-

      - name: Restore coverage cache
        if: env.IS_FULL_RUN != 'true'
        uses: actions/cache/restore@v4
        with:
          path: .coverage.baseline
          key: coverage-main-${{ github.sha }}
          restore-keys: |
            coverage-main-

      - name: Copy baseline coverage for combining
        if: env.IS_FULL_RUN != 'true'
        run: |
          if [ -f .coverage.baseline ]; then
            echo "Baseline coverage found, will combine with PR delta"
            cp .coverage.baseline coverage_main.dat
          else
            echo "No baseline coverage found, PR will generate full coverage"
          fi

      # ========================================================================
      # TEST EXECUTION
      # ========================================================================
      # On full runs: single pass rebuilds testmon database and collects coverage
      # On PR runs: testmon --testmon-nocollect selects tests without updating db

      - name: Run all tests (full run)
        if: env.IS_FULL_RUN == 'true'
        run: |
          export PATH="$HOME/.local/bin:$PATH"
          # Remove stale testmon data to force full rebuild
          rm -f .testmondata .testmondata-shm .testmondata-wal
          rm -f .coverage .coverage.*
          # Single pass: rebuild testmon database AND collect coverage
          # Keep threshold enforcement centralized in the shared report step.
          uv run ${CI_UV_EXTRAS} pytest --cov=nvalchemi --cov-report= --testmon test/

      - name: Run selective tests (PR)
        if: env.IS_FULL_RUN != 'true'
        run: |
          export PATH="$HOME/.local/bin:$PATH"
          # Run only affected tests using testmon with --testmon-nocollect
          # This selects tests based on cached db but doesn't update it
          # Keep fail-under disabled here; threshold is enforced in shared
          # coverage reporting after canonicalization.
          export COVERAGE_FILE=coverage_pr.dat
          uv run ${CI_UV_EXTRAS} pytest --cov=nvalchemi --cov-report= --cov-fail-under=0 --testmon --testmon-nocollect test/
          if [ ! -f coverage_pr.dat ]; then
            echo "No PR coverage produced from selective run; falling back to full test suite"
            uv run ${CI_UV_EXTRAS} pytest --cov=nvalchemi --cov-report= --cov-fail-under=0 test/
          fi

      # ========================================================================
      # UMA TESTS (isolated environment)
      # ========================================================================
      # UMA can't share the cu13/mace env, so its tests run from
      # .venv-uma-cu12 and append coverage to the active run's data file.
      # Structural tests always
      # run; the checkpoint-based tests skip without HF access to facebook/UMA
      # (set the HF_TOKEN secret to exercise them). The ``@slow`` NVE / turbo
      # tests are not selected (no --slow), matching the main suite.

      - name: Run UMA tests (.venv-uma-cu12)
        if: needs.changed-files.outputs.uma_changed == 'true' || env.IS_FULL_RUN == 'true'
        env:
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
        run: |
          export PATH="$HOME/.local/bin:$PATH"
          if [ "$IS_FULL_RUN" = "true" ]; then
            export COVERAGE_FILE=.coverage
          else
            export COVERAGE_FILE=coverage_pr.dat
          fi
          UV_PROJECT_ENVIRONMENT=.venv-uma-cu12 uv run --no-sync \
            pytest --cov=nvalchemi --cov-append --cov-report= --cov-fail-under=0 \
            test/models/test_uma.py

      # ========================================================================
      # COVERAGE REPORTING (shared for full runs and PR runs)
      # ========================================================================

      - name: Canonicalize coverage output
        run: |
          export PATH="$HOME/.local/bin:$PATH"
          if [ "$IS_FULL_RUN" = "true" ]; then
            if [ -f .coverage ]; then
              echo "Using full-run coverage output"
            else
              echo "Warning: Full run produced no coverage data"
              exit 1
            fi
          else
            # Combine baseline (if exists) with PR coverage, then canonicalize to .coverage
            if [ -f coverage_main.dat ] && [ -f coverage_pr.dat ]; then
              echo "Combining baseline and PR coverage"
              uv run ${CI_UV_EXTRAS} coverage combine --data-file=.coverage coverage_main.dat coverage_pr.dat
            elif [ -f coverage_pr.dat ]; then
              echo "Using PR coverage only (no baseline)"
              mv coverage_pr.dat .coverage
            elif [ -f coverage_main.dat ]; then
              echo "Using baseline coverage only"
              mv coverage_main.dat .coverage
            else
              echo "Warning: No coverage data available"
              exit 1
            fi
          fi

      - name: Generate coverage XML
        run: |
          export PATH="$HOME/.local/bin:$PATH"
          uv run ${CI_UV_EXTRAS} coverage xml --fail-under=0 -o nvalchemi.coverage.xml

      - name: Check coverage threshold
        run: |
          export PATH="$HOME/.local/bin:$PATH"
          uv run ${CI_UV_EXTRAS} coverage report

      # ========================================================================
      # CACHE SAVE (full runs only: main/schedule/merge_group)
      # ========================================================================
      # Save testmon database and coverage baseline for future PR runs

      - name: Save testmon cache
        if: env.IS_FULL_RUN == 'true'
        uses: actions/cache/save@v4
        with:
          path: |
            .testmondata
            .testmondata-shm
            .testmondata-wal
          key: testmon-main-${{ github.sha }}

      - name: Prepare coverage baseline
        if: env.IS_FULL_RUN == 'true'
        run: |
          cp .coverage .coverage.baseline

      - name: Save coverage cache
        if: env.IS_FULL_RUN == 'true'
        uses: actions/cache/save@v4
        with:
          path: .coverage.baseline
          key: coverage-main-${{ github.sha }}

      # ========================================================================
      # UPLOAD ARTIFACTS
      # ========================================================================

      - name: Upload coverage to Codecov
        if: github.event_name != 'merge_group' && github.event_name != 'schedule'
        uses: codecov/codecov-action@v5
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          files: ./nvalchemi.coverage.xml
          fail_ci_if_error: false

      - name: Upload coverage report artifact
        uses: actions/upload-artifact@v4
        with:
          name: coverage-report
          path: nvalchemi.coverage.xml
          retention-days: 7

  # ============================================================================
  # MULTI-GPU TEST STAGE (2-GPU Runner)
  # ============================================================================
  # No testmon: selection is the marker, and a second writer would corrupt the
  # cached db the 1-GPU job maintains. No coverage: appending from a separate
  # job would desync the baseline/PR combine above.
  test-multigpu:
    needs:
      - lint
      - get-pr-labels
      - changed-files
    # Overridable via a repo variable so a runner-fleet rename is a settings
    # edit, not a code change; an unknown label queues silently until GitHub
    # times it out ~24h later, with no runner ever assigned.
    runs-on: ${{ vars.MULTIGPU_RUNNER || 'linux-amd64-gpu-h100-latest-2' }}
    # DD equivalence + compile recompile gates dominate; UMA adds a second
    # torch install and a possible checkpoint download.
    timeout-minutes: 90
    container:
      image: nvcr.io/nvidia/cuda:13.1.0-runtime-ubuntu24.04
      # NCCL and mp.spawn need more than the 64 MB default /dev/shm.
      options: --gpus all --shm-size=8g
    env:
      IS_FULL_RUN: ${{ github.ref == 'refs/heads/main' || github.event_name == 'schedule' || github.event_name == 'merge_group' }}
      NCCL_DEBUG: WARN
    if: |
      (github.event_name == 'schedule') ||
      contains(fromJSON(needs.get-pr-labels.outputs.labels || '[]'), 'ciflow:all') ||
      contains(fromJSON(needs.get-pr-labels.outputs.labels || '[]'), 'ci:multigpu') ||
      (
        !contains(fromJSON(needs.get-pr-labels.outputs.labels || '[]'), 'ciflow:skip') &&
        (needs.changed-files.outputs.dd_changed == 'true')
      ) ||
      (
        (github.event_name == 'merge_group') &&
        (needs.changed-files.outputs.dd_changed == 'true')
      )
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install apt requirements
        env:
          DEBIAN_FRONTEND: "noninteractive"
          TZ: "Etc/UTC"
        run: |
          apt-get update && \
          apt-get install -y curl \
            git \
            build-essential

      - name: Setup UV
        env:
          UV_VERSION: "0.9.25"
          UV_CHECKSUM: "10fb1f54d56f3eb60622006797339d4ea0bfda9b358d07db635f73cf89f7094c"
        run: |
          UV_INSTALLER=$(mktemp)
          curl --proto '=https' --tlsv1.2 -LsSf "https://astral.sh/uv/${UV_VERSION}/install.sh" -o "$UV_INSTALLER"
          echo "${UV_CHECKSUM}  ${UV_INSTALLER}" | sha256sum -c -
          sh "$UV_INSTALLER"
          rm "$UV_INSTALLER"
          echo "$HOME/.local/bin" >> $GITHUB_PATH

      - name: Install dependencies
        run: |
          export PATH="$HOME/.local/bin:$PATH"
          uv sync ${CI_UV_EXTRAS}

      # Without this, a runner short a device would skip every test and
      # report green.
      - name: Verify 2 GPUs are visible
        run: |
          export PATH="$HOME/.local/bin:$PATH"
          nvidia-smi
          uv run ${CI_UV_EXTRAS} python -c "
          import sys, torch
          n = torch.cuda.device_count()
          print(f'visible CUDA devices: {n}')
          if n < 2:
              sys.exit(f'expected >=2 GPUs on this runner, found {n}')
          "

      - name: Run multi-GPU tests
        run: |
          export PATH="$HOME/.local/bin:$PATH"
          uv run ${CI_UV_EXTRAS} pytest -m multigpu test/

      # ========================================================================
      # UMA MULTI-GPU TESTS (isolated environment)
      # ========================================================================
      # Same CUDA 12 split as the 1-GPU job. These checkpoint tests must fail,
      # rather than skip, when gated CI cannot load UMA.

      - name: Install UMA CUDA 12 environment (.venv-uma-cu12)
        if: needs.changed-files.outputs.uma_changed == 'true' || env.IS_FULL_RUN == 'true'
        run: |
          export PATH="$HOME/.local/bin:$PATH"
          uv python install 3.12
          UV_PROJECT_ENVIRONMENT=.venv-uma-cu12 uv sync --frozen --python 3.12 \
            --extra uma-cu12 --extra ase --no-group build

      - name: Verify UMA CUDA 12 environment and 2 GPUs
        if: needs.changed-files.outputs.uma_changed == 'true' || env.IS_FULL_RUN == 'true'
        run: |
          export PATH="$HOME/.local/bin:$PATH"
          UV_PROJECT_ENVIRONMENT=.venv-uma-cu12 uv run --no-sync python -c "
          import importlib.metadata as md, sys, torch
          fairchem = md.version('fairchem-core')
          devices = torch.cuda.device_count()
          print(f'python={sys.version.split()[0]} fairchem-core={fairchem} torch={torch.__version__} cuda={torch.version.cuda} devices={devices}')
          if sys.version_info[:2] != (3, 12):
              sys.exit(f'expected Python 3.12, found {sys.version.split()[0]}')
          if not fairchem.startswith('2.22.'):
              sys.exit(f'expected fairchem-core 2.22.x, found {fairchem}')
          if not torch.__version__.startswith('2.13.0+cu126'):
              sys.exit(f'expected torch 2.13.0+cu126, found {torch.__version__}')
          if not str(torch.version.cuda).startswith('12.'):
              sys.exit(f'expected a CUDA 12 torch build, found {torch.version.cuda}')
          if devices < 2:
              sys.exit(f'expected >=2 GPUs on this runner, found {devices}')
          "

      - name: Run UMA multi-GPU tests (.venv-uma-cu12)
        if: needs.changed-files.outputs.uma_changed == 'true' || env.IS_FULL_RUN == 'true'
        env:
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
        run: |
          export PATH="$HOME/.local/bin:$PATH"
          NVALCHEMI_FORCE_MULTIGPU=1 \
            UV_PROJECT_ENVIRONMENT=.venv-uma-cu12 uv run --no-sync \
            pytest -m multigpu \
            test/distributed/model/test_uma_multigpu.py \
            test/distributed/model/test_uma_gp_partition_multigpu.py

  # ============================================================================
  # VERIFY STATUS
  # ============================================================================
  verify-status:
    needs:
      - lint
      - get-pr-labels
      - test
      - test-multigpu
    runs-on: ubuntu-latest
    if: always()
    steps:
      - name: Check job statuses
        run: |
          if [[ "${{ contains(needs.*.result, 'failure') || contains(needs.*.result, 'cancelled') }}" == "true" ]]; then
            echo "Some jobs have failed or been cancelled!"
            exit 1
          else
            echo "All jobs have completed successfully or been skipped!"
            exit 0
          fi
